<header>

</header>

<div class="container">
  <h1>MVCheezburger</h1>
</div>

<div class="container">
  <div class="item burger-tray top-box left-box">
    <h2 class="tray-title">Cheezburgers remaining</h2>
    <ul>
      {{#unless burgers}}
      <li><p>There are no burgers yet! Time to place an order...</p></li>
      {{/unless}}
      {{#each burgers}}
        {{#unless devoured}}
          {{> burgers/burger-block devoured=1}}
        {{/unless}}
      {{/each}}
    </ul>
  </div>
  <div class="item burger-tray">
    <h2 class="tray-title">Echoes of cheezburgers past</h2>
    <ul>
      {{#unless burgers}}
      <li>No devoured yet</li>
      {{/unless}}
      {{#each burgers}}
        {{#if devoured}}
          {{> burgers/burger-block devoured=0}}
        {{/if}}
      {{/each}}
    </ul>
  </div>
</div>

<div class="container">
  <h2>Order a Burger</h2>
</div>

<div class="container">
  <form class="create-form">
    <div class="form-group">
      <label for="burger">Order a Burger:</label>
      <input type="text" id="burger" name="burger_name">
    </div>

    <button type="submit">Place Order</button>
  </form>
</div>
